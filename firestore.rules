rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Geçici debug kuralı - tam açık
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
